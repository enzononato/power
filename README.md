# power
<li>check if a number is a power of any base with "if-else" <br><br>
the user will inform the desired base and the number to be represented in said base. The program checks whether or not the number is a power of the base informed and displays the output.
